export enum ThemeEnum {
  SP = 'sp',
  EP = 'ep',
  ADMIN = 'admin',
}

export enum ModeEnum {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum ThemeColorEnum {
  SP = '#289e22',
  EP = '#409EFF',
  ADMIN = '#409EFF',
}
